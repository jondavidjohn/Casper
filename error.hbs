{{!< default}}

{{! This is a page template. A page outputs content just like any other post, and has all the same
    attributes by default, but you can also customise it to behave differently if you prefer. }}

<main class="content" role="main">

    <article>

        <header class="post-header">
            <a class="blog-logo" href="{{@blog.url}}">
                {{#if @blog.logo}}
                    <img src="{{@blog.logo}}" alt="Blog Logo" />
                {{else}}
                    <span class="blog-title">{{@blog.title}}</span>
                {{/if}}
            </a>
        </header>

            <h1 class="post-title">404 - Page Not Found</h1>

            <section class="post-content">
                I'm sorry you've somehow made it to a page here that is no longer available.  You can view my other posts at <a href="/">jondavidjohn.com</a>, but you probably are going to move on.  In any case, sorry what you were looking for wasn't here, <strong>godspeed</strong>!
            </section>

    </article>

</main>
